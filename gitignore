# Dependencies — never commit these, Vercel must install fresh
node_modules/
.pnp
.pnp.js

# Production build
build/

# Lock files from Stackblitz (can conflict with Vercel)
# Keep package-lock.json only if you generated it locally with Node 18+
# package-lock.json

# Environment
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.DS_Store
.idea/
*.swp
